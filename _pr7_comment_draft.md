Matt — here's where each of your review concerns landed after the 2026-05-06→07 rebuild. The PR has changed structurally since you last reviewed (the leaning toward D has been retracted, the scoring was re-derived from proposal 105's principles, and an empirical field census against the authoritative spec surfaces was added), so this rolls forward what's responsive to your specific concerns. Only your redline verifies whether each one is actually addressed.

**1. Collision detection isn't more silent in A than in B.** §3.3 governance reframed to *"the difference is what a registry-spec validator has to parse, not whether collisions are silent."* §5.1 vendor simulation has an explicit collision callout: *"the mechanism does not prevent the collision; the registry process does."* Collision detection is treated symmetrically across A/B/C/D in the rebuilt scoring.

**2. Composition framing was pessimistic for dicts, optimistic for schemas.** §3.1 leads with *"composition behavior is effectively equivalent across the four approaches at the granularity authors actually use."* Round-trip-rewrite hazard reframed as a tool-author bug rather than a structural property. **Composability scored 4/4/4/4 across A/B/C/D** in the rebuilt scoring (one of the eight principle-derived dimensions).

**3. Schema governance/validation was overweighted.** §3.3 explicitly notes authors can list arbitrary strings under `apiSchemas`, and governance enforceability is treated as symmetric across mechanisms. The rebuilt scoring re-derives dimensions from the proposal's eight authorized principles rather than from candidate profiles, so schema-only advantages no longer get crediting that doesn't survive the experiments.

**4. Schema-distribution friction was assumed away.** Resolved as conditional in the 2026-05-07 rebuild: distribution friction is currently elevated (the matrix burden — DCC × USD release × Python × OS × runtime × build flavor, fragmented across vendors who ship USD binaries today) and trending lighter as the AOUSD Build IG epic ([`aousd/build-ig-initiatives#28`](https://github.com/aousd/build-ig-initiatives/issues/28)) lands. Reflected in the *Minimal disruption* score for B and C in §3.6. Framing detail in [`details/formality_and_distribution.md`](extras/sourceIdentifiers/details/formality_and_distribution.md).

**5. Semantic Labels + minimal identity dict.** Added as Approach D, framed initially as a refinement of B that reuses the existing `UsdSemanticsLabelsAPI`. **The comparison initially leaned toward D; that leaning has been retracted.** A pre-registered field census against the authoritative spec surfaces (IFC4x3, Revit API, AAS metamodel, Windchill REST, SAP MARA, ROS/URDF/SDF, OpenAssetIO/MovieLabs OMC/ShotGrid) — `details/field_classification_experiment.md` — shows identifier packages are heterogeneously typed across all four verticals: timestamps universal; numeric measures with units in three of four; composite typed references universal; polymorphic XSD-typed values the standard surface in AAS. D's `token[]`-only label surface + identifier strings cannot carry that surface. D is preserved as an explored idea (the scenarios `column_d`, `manufacturing_d`, `robotics_d` and the 8-vendor sim still exist) but with a provenance note in `approach_descriptions.md` clarifying it was constructed downstream of the proposal, not authorized as a peer candidate.

**6. `apiSchemas` list scaling — the linear-sift complaint.** §3.2 has a paragraph crediting the cost; same penalty applies to B/C/D symmetrically. Listed in §4 Open Questions: mechanisms that put per-facet or per-system instances on `apiSchemas` (D, and B/C in different ways) inherit the linear-sift cost the physics workflows already feel.

**7. Identifier-terminology question.** The doc text uses the domain-specific terms each system actually publishes (IFC GlobalId, Revit ElementId, Windchill OID, ROS frame ID, etc.) rather than generic "GUID" framing. Stress-test code references `ifc_guid` because that is IFC's own term. Happy to revisit.

**Cross-cutting changes since you last looked:**

- A and B remain the two foundations proposal 105 authorized; C is the proposal-hinted "hybrid or alternative"; D is downstream-constructed and now flagged as such. The comparison reads symmetrically across A/B/C/D with no mechanism leaning asserted.
- Rebuilt scoring totals (max 40, derived from the proposal's eight authorized principles): **A=32, B=26, C=32, D=33**. A/C/D within scoring noise (3-point spread); B trails meaningfully because the four-property fixed surface admits only a common subset. The legacy ad-hoc scoring (eight dimensions invented during PR development, biased toward D by construction) is preserved at `stress_tests/vendor_adoption_analysis_legacy.py` for inspection.
- The methodology journey from leaning-toward-D through empirical census to principle-derived rebuild — and the methodology lessons that may travel beyond this work — is captured as a [PR comment](https://github.com/asluk/USD/pull/7#issuecomment-4399081816) rather than in the body.
