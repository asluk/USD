#usda 1.0
(
    "WARNING: THIS FILE IS GENERATED BY usdGenSchema.  DO NOT EDIT."
)

class CoordinateReferenceSystem "CoordinateReferenceSystem" (
    customData = {
        string userDocBrief = "A coordinate reference system."
    }
)
{
    uniform string crs:displayName = "" (
        customData = {
            string userDocBrief = "Human-readable name for tools/UI."
        }
    )
    uniform double crs:epoch = 0 (
        customData = {
            string userDocBrief = "Optional coordinate epoch (decimal year, e.g."
        }
    )
    uniform int crs:epsg = 0 (
        customData = {
            string userDocBrief = "Optional EPSG code."
        }
    )
    asset[] crs:gridFiles = [] (
        customData = {
            string userDocBrief = "Optional PROJ transformation-grid asset paths (e.g."
        }
    )
    uniform token crs:wkt = "" (
        customData = {
            string userDocBrief = "OGC WKT2_2019 definition of this CRS."
        }
    )
}

class "CRSBindingAPI" (
    customData = {
        string userDocBrief = """Applied to a prim to give it a geospatial position in a bound CRS,
    analogous to UsdShadeMaterialBindingAPI."""
    }
)
{
    rel crs:binding (
        customData = {
            string userDocBrief = """All-purpose binding to the CoordinateReferenceSystem prim that
        crs:position is expressed in."""
        }
    )
    double3 crs:position = (0, 0, 0) (
        customData = {
            string userDocBrief = """Absolute position in the bound CRS, ALWAYS ordered
        (x = longitude/easting, y = latitude/northing, z = height) regardless of
        the bound CRS authority's declared axis order (east-north-up / always_xy)."""
        }
    )
}

